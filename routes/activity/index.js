const router = require("express").Router()
const controller = require("./activity.controller")

/* 이벤트 로그 저장 */
router.post("/",)

/* bid로 이벤트 조회 */
router.get("/board/:bid",)

/* cid로 이벤트 조회 */
router.post("/card/:cid",)

